<%= render_custom_table_tabs custom_table_tabs(@project.id), @tab %>

<% if @custom_table.custom_fields.any? || @custom_table.editable? %>
    <% content_for :sidebar do %>
        <%= render partial: 'sidebar' %>
    <% end %>
<% end %>

<% if @custom_table.custom_fields.any? %>
    <% other_formats_links do |f| %>
        <%= call_hook(:view_custom_tables_show_other_formats_links_top, f: f, url: params, custom_table: @custom_table)  %>
        <%= f.link_to 'PDF', :url => params %>
        <%= f.link_to_with_query_parameters 'CSV', {}, :onclick => "showModal('csv-export-options', '350px'); return false;" %>
    <% end %>
<% end %>

<div id="csv-export-options" style="display:none;">
  <h3 class="title"><%= l(:label_export_options, :export_format => 'CSV') %></h3>
  <%= form_tag(project_custom_table_path(project_id: @project.id, :format => 'csv'), :method => :get, :id => 'csv-export-form') do %>
      <%= query_as_hidden_field_tags(@query) %>
      <p>
        <label><%= radio_button_tag 'c[]', '', true %> <%= l(:description_selected_columns) %></label><br />
        <label><%= radio_button_tag 'c[]', 'all_inline' %> <%= l(:description_all_columns) %></label>
      </p>
      </p>
      <p class="buttons">
        <%= submit_tag l(:button_export), :name => nil, :onclick => "hideModal(this);" %>
        <%= submit_tag l(:button_cancel), :name => nil, :onclick => "hideModal(this);", :type => 'button' %>
      </p>
  <% end %>
</div>
